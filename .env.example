# Hostinger API token used by Traefik's Let's Encrypt DNS-01 resolver.
# Required permissions must allow DNS zone record management for fitssort.com.
HOSTINGER_API_TOKEN=replace-with-hostinger-api-token