{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Documentation","text":"<p>Setup and operations guides for the Traefik infrastructure stack.</p> Guide Description Getting Started Prerequisites, environment variables, startup order DNS &amp; Domains Cloudflare DNS for all platform zones TLS &amp; Certificates Origin certificates and Let's Encrypt fallback Monitoring Prometheus, Grafana, Loki, and Promtail Sentry Cloud Hosted error tracking (recommended for 12 GB RAM) Self-Hosted Sentry On-prem Sentry via official installer (16 GB+ RAM) Routing &amp; ADMS Docker labels, middlewares, biometric device HTTP Troubleshooting Common issues and fixes","path":["Documentation"],"tags":[]},{"location":"dns-and-domains/","level":1,"title":"DNS &amp; Domains","text":"<p>All zones are expected to be proxied through Cloudflare with SSL/TLS mode: Full (strict).</p>","path":["DNS &amp; Domains"],"tags":[]},{"location":"dns-and-domains/#musfiqdehancom","level":2,"title":"musfiqdehan.com","text":"<p>Apex only — no <code>*.musfiqdehan.com</code> tenant wildcard.</p> Record Target <code>musfiqdehan.com</code> Server IP (A/AAAA) <p>Add explicit hostnames as needed (e.g. <code>grafana.musfiqdehan.com</code>).</p>","path":["DNS &amp; Domains"],"tags":[]},{"location":"dns-and-domains/#tenant-platform-domains-wildcard","level":2,"title":"Tenant platform domains (wildcard)","text":"<p>Wildcard DNS does not cover the apex. For each UK zone, create both apex and wildcard records:</p> Zone Apex Wildcard <code>mrdfit.uk</code> <code>mrdfit.uk</code> → server <code>*.mrdfit.uk</code> → server <code>mrderp.uk</code> <code>mrderp.uk</code> → server <code>*.mrderp.uk</code> → server <code>mrdhrms.uk</code> <code>mrdhrms.uk</code> → server <code>*.mrdhrms.uk</code> → server <code>mrdlms.uk</code> <code>mrdlms.uk</code> → server <code>*.mrdlms.uk</code> → server <p>Proxy both records (orange cloud) to the same origin.</p>","path":["DNS &amp; Domains"],"tags":[]},{"location":"dns-and-domains/#stale-records","level":2,"title":"Stale records","text":"<p>Remove any old A/AAAA/CNAME records pointing to Hostinger parking, a previous VPS, or another host. Mixed records cause intermittent routing to the wrong origin.</p>","path":["DNS &amp; Domains"],"tags":[]},{"location":"dns-and-domains/#http-redirect-rules","level":2,"title":"HTTP redirect rules","text":"<p>Port-80 → HTTPS redirects for <code>/api</code>, <code>/ninja</code>, <code>/media</code>, <code>/static</code>, and <code>/ws</code> are defined in dynamic/redirects.yml:</p> <ul> <li>Platform hosts: <code>musfiqdehan.com</code> (apex), plus apex + <code>*.</code> for each UK zone above</li> <li>Custom domains: all other hostnames (tenant-owned domains)</li> </ul> <p>ADMS device paths (<code>/iclock</code>, <code>/cdata</code>, etc.) stay on plain HTTP — see Routing &amp; ADMS.</p>","path":["DNS &amp; Domains"],"tags":[]},{"location":"dns-and-domains/#cloudflare-api-token","level":2,"title":"Cloudflare API token","text":"<p>For Let's Encrypt DNS-01, create a token with Zone → DNS → Edit and Zone → Zone → Read scoped to every zone:</p> <p><code>musfiqdehan.com</code>, <code>mrdfit.uk</code>, <code>mrderp.uk</code>, <code>mrdhrms.uk</code>, <code>mrdlms.uk</code></p> <p>Set as <code>CF_DNS_API_TOKEN</code> in .env.</p>","path":["DNS &amp; Domains"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting Started","text":"","path":["Getting Started"],"tags":[]},{"location":"getting-started/#prerequisites","level":2,"title":"Prerequisites","text":"<ul> <li>Docker and Docker Compose</li> <li>Domains managed in Cloudflare</li> <li>Origin certificates and/or a Cloudflare API token for Let's Encrypt DNS-01</li> <li>Application stacks (Django, Next.js, etc.) configured to join the <code>traefik_proxy</code> network</li> </ul>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#environment-variables","level":2,"title":"Environment variables","text":"<p>Copy the example file and fill in secrets:</p> <pre><code>cp .env.example .env\n</code></pre> Variable Used by Purpose <code>CF_DNS_API_TOKEN</code> Traefik Let's Encrypt DNS-01 for all managed zones <code>GRAFANA_ADMIN_USER</code> Grafana Admin username (default <code>admin</code>) <code>GRAFANA_ADMIN_PASSWORD</code> Grafana Admin password for Grafana UI <p>See .env.example for details.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#grafana-edge-basic-auth","level":2,"title":"Grafana edge basic auth","text":"<p>Before starting the monitoring stack:</p> <pre><code>htpasswd -cb dynamic/.htpasswd admin 'your-strong-password'\n</code></pre> <p>This file is gitignored. See dynamic/.htpasswd.example.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#startup-order","level":2,"title":"Startup order","text":"<pre><code># 1. Traefik — creates traefik_proxy network\ndocker compose --env-file .env -f docker-compose.traefik.yml up -d\n\n# 2. Monitoring — metrics and logs\ndocker compose --env-file .env -f docker-compose.monitoring.yml up -d\n\n# 3. Application stacks (from their respective repositories)\n# docker compose -f &lt;app&gt;/docker-compose.prod.yml up -d --build\n</code></pre> <p>Traefik must start first so <code>traefik_proxy</code> exists for app and monitoring containers.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#related-guides","level":2,"title":"Related guides","text":"<ul> <li>DNS &amp; Domains</li> <li>TLS &amp; Certificates</li> <li>Monitoring</li> </ul>","path":["Getting Started"],"tags":[]},{"location":"monitoring/","level":1,"title":"Monitoring Stack","text":"<p>Docker-based observability: Prometheus (metrics), Grafana (dashboards), Loki + Promtail (container logs).</p>","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#components","level":2,"title":"Components","text":"Service Image role Exposure Prometheus Metrics store (15-day retention) Internal only Grafana UI at <code>https://grafana.musfiqdehan.com</code> Via Traefik + basic auth Loki Log store (14-day retention) Internal only Promtail Ships all Docker json-file logs to Loki Internal only node-exporter Host CPU, RAM, disk, network Internal only cAdvisor Per-container resource usage Internal only <p>Config lives under monitoring/. Compose file: docker-compose.monitoring.yml.</p> <p>On a 12 GB RAM server, this stack plus Traefik and application containers is reasonable.</p>","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#prerequisites","level":2,"title":"Prerequisites","text":"<ol> <li>Traefik running (<code>traefik_proxy</code> network exists)</li> <li><code>dynamic/.htpasswd</code> created — see Getting Started</li> <li><code>GRAFANA_ADMIN_PASSWORD</code> set in <code>.env</code></li> </ol>","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#start","level":2,"title":"Start","text":"<pre><code>docker compose --env-file .env -f docker-compose.monitoring.yml up -d\n</code></pre>","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#grafana-access","level":2,"title":"Grafana access","text":"Layer Credentials Traefik edge <code>dynamic/.htpasswd</code> (htpasswd user/password) Grafana app <code>GRAFANA_ADMIN_USER</code> / <code>GRAFANA_ADMIN_PASSWORD</code> from <code>.env</code> <p>URL: <code>https://grafana.musfiqdehan.com</code></p> <p>Prometheus and Loki datasources are auto-provisioned from monitoring/grafana/provisioning/.</p>","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#recommended-dashboards","level":2,"title":"Recommended dashboards","text":"<p>Import by ID in Grafana → Dashboards → Import:</p> ID Name 1860 Node Exporter Full 193 Docker cAdvisor 11462 Traefik 2.x (compatible with Traefik 3 metrics)","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#log-queries-explore-loki","level":2,"title":"Log queries (Explore → Loki)","text":"<pre><code>{container_name=~\".+\"}\n{compose_service=\"backend\"}\n{container_name=~\".*postgres.*|.*redis.*\"}\n</code></pre> <p>Promtail reads <code>/var/lib/docker/containers</code> and the Docker socket — all containers on the host that use the default <code>json-file</code> log driver are included.</p>","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#verification","level":2,"title":"Verification","text":"<pre><code>docker run --rm --network traefik_proxy curlimages/curl:latest \\\n  -s http://prometheus:9090/-/healthy\n\ndocker run --rm --network traefik_proxy curlimages/curl:latest \\\n  -s http://traefik:8080/metrics | head\n</code></pre> <p>Traefik exposes Prometheus metrics on internal entrypoint <code>:8080</code> — see traefik.yml.</p>","path":["Monitoring Stack"],"tags":[]},{"location":"monitoring/#security","level":2,"title":"Security","text":"<ul> <li>Do not publish Prometheus, Loki, or cAdvisor ports on the host</li> <li>Do not add Traefik labels to internal monitoring services except Grafana</li> </ul>","path":["Monitoring Stack"],"tags":[]},{"location":"routing-and-adms/","level":1,"title":"Routing &amp; ADMS","text":"","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#how-traefik-discovers-services","level":2,"title":"How Traefik discovers services","text":"<p>Containers must:</p> <ul> <li>Join the <code>traefik_proxy</code> network</li> <li>Set <code>traefik.enable=true</code></li> <li>Define router rules (<code>Host(...)</code>, <code>HostRegexp(...)</code>)</li> <li>Point to the correct backend port</li> <li>Optionally set <code>tls.certresolver=letsencrypt</code> with explicit <code>tls.domains</code></li> </ul> <p>Example labels:</p> <pre><code>labels:\n  - traefik.enable=true\n  - traefik.http.routers.api.rule=Host(`api.musfiqdehan.com`)\n  - traefik.http.routers.api.entrypoints=websecure\n  - traefik.http.routers.api.tls=true\n  - traefik.http.routers.api.tls.certresolver=letsencrypt\n  - traefik.http.services.api.loadbalancer.server.port=8000\n  - traefik.http.routers.api.middlewares=security-headers@file,compress@file\n</code></pre> <p>For UK tenant subdomains, use <code>HostRegexp</code> in application compose files, e.g. <code>^[a-z0-9][a-z0-9-]*\\.mrdfit\\.uk$</code>.</p>","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#middlewares","level":2,"title":"Middlewares","text":"<p>Defined in dynamic/middlewares.yml. Reference as <code>&lt;name&gt;@file</code> in labels.</p> Middleware Purpose <code>security-headers@file</code> HSTS, XSS protection, <code>X-Forwarded-Proto: https</code> <code>adms-http-headers@file</code> Plain HTTP for devices — no HSTS, <code>X-Forwarded-Proto: http</code> <code>compress@file</code> Response compression <code>api-ratelimit@file</code> 100 req/s average, burst 50 <code>ws-headers@file</code> WebSocket upgrade passthrough <code>redirect-to-https@file</code> HTTP → HTTPS scheme redirect <code>monitoring-basic-auth@file</code> Basic auth for Grafana edge","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#adms-device-routing-http-only","level":2,"title":"ADMS device routing (HTTP-only)","text":"<p>ZKTeco iClock biometric devices poll over plain HTTP on port 80. Browsers and APIs use HTTPS on port 443.</p> <pre><code>Device  → http://{host}/iclock/cdata?SN=...  → Traefik :80 → Daphne :8021\nBrowser → https://{host}/api/v1/...          → Traefik :443 (rate limited)\n</code></pre>","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#router-split-backend-labels","level":3,"title":"Router split (backend labels)","text":"Router Entrypoint Paths Rate limit <code>backend-api</code> websecure (443) <code>/api</code>, <code>/ninja</code>, <code>/media</code>, <code>/static</code> Yes <code>backend-ws</code> websecure (443) <code>/ws</code> No <code>backend-adms</code> web (80) <code>/iclock</code>, <code>/cdata</code>, <code>/getrequest</code>, <code>/devicecmd</code> No <code>backend-custom-*</code> same pattern custom tenant domains same <p>HTTPS ADMS paths are intentionally not routed.</p>","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#device-url-format","level":3,"title":"Device URL format","text":"<pre><code>http://{tenant}.mrdfit.uk/iclock\nhttp://{tenant}.mrderp.uk/iclock\nhttp://musfiqdehan.com/iclock\nhttp://{custom-domain}/iclock\n</code></pre> <p>Supported endpoints:</p> <ul> <li><code>/iclock/cdata</code> or <code>/cdata</code> — heartbeat + attendance</li> <li><code>/iclock/getrequest</code> or <code>/getrequest</code> — command poll</li> <li><code>/iclock/devicecmd</code> or <code>/devicecmd</code> — command acknowledgement</li> </ul>","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#smoke-test","level":3,"title":"Smoke test","text":"<pre><code>curl -v \"http://tenant.mrdfit.uk/iclock/cdata?SN=YOUR_SN\"    # must succeed\ncurl -v \"https://tenant.mrdfit.uk/iclock/cdata?SN=YOUR_SN\"   # must not reach ADMS\n</code></pre>","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#cloudflare","level":3,"title":"Cloudflare","text":"<p>If domains are proxied, ensure port 80 reaches the origin for device hostnames (grey-cloud device subdomains, or rules allowing HTTP on <code>/iclock/*</code>).</p>","path":["Routing &amp; ADMS"],"tags":[]},{"location":"routing-and-adms/#port-80-selective-redirect","level":2,"title":"Port 80 selective redirect","text":"<p>dynamic/redirects.yml redirects <code>/api</code>, <code>/ninja</code>, <code>/media</code>, <code>/static</code>, and <code>/ws</code> to HTTPS on platform hosts while leaving ADMS paths on HTTP.</p>","path":["Routing &amp; ADMS"],"tags":[]},{"location":"sentry-cloud/","level":1,"title":"Sentry Cloud","text":"<p>Recommended for servers with limited RAM (e.g. 12 GB). No containers run on this host for Sentry.</p> <p>Sentry captures application errors (exceptions, stack traces, releases). It does not replace Loki for raw container logs — use Monitoring for log aggregation.</p>","path":["Sentry Cloud"],"tags":[]},{"location":"sentry-cloud/#setup","level":2,"title":"Setup","text":"<ol> <li>Create an account at sentry.io</li> <li>Create an organization</li> <li>Create one project per application (Django API, Next.js frontend, NestJS API, etc.)</li> <li>Copy each project's DSN from Settings → Projects → [project] → Client Keys (DSN)</li> </ol> <p>No Traefik routes or DNS records are required — apps send events outbound to Sentry's ingest endpoints.</p>","path":["Sentry Cloud"],"tags":[]},{"location":"sentry-cloud/#sdk-wiring","level":2,"title":"SDK wiring","text":"App Package Environment variable Django backend <code>sentry-sdk[django]</code> <code>SENTRY_DSN</code>, <code>SENTRY_ENVIRONMENT</code> Next.js frontend <code>@sentry/nextjs</code> <code>NEXT_PUBLIC_SENTRY_DSN</code> NestJS API <code>@sentry/nestjs</code> <code>SENTRY_DSN</code> <p>DSN format:</p> <pre><code>https://&lt;key&gt;@o&lt;org-id&gt;.ingest.&lt;region&gt;.sentry.io/&lt;project-id&gt;\n</code></pre>","path":["Sentry Cloud"],"tags":[]},{"location":"sentry-cloud/#django-example","level":3,"title":"Django example","text":"<pre><code>import os\nimport sentry_sdk\n\nsentry_sdk.init(\n    dsn=os.environ[\"SENTRY_DSN\"],\n    environment=os.environ.get(\"SENTRY_ENVIRONMENT\", \"production\"),\n    traces_sample_rate=0.1,\n)\n</code></pre>","path":["Sentry Cloud"],"tags":[]},{"location":"sentry-cloud/#when-to-use-self-hosted-instead","level":2,"title":"When to use self-hosted instead","text":"<p>See Self-Hosted Sentry if you have 16 GB+ RAM (24 GB+ with monitoring) and need data residency or air-gapped deployment.</p>","path":["Sentry Cloud"],"tags":[]},{"location":"sentry-self-hosted/","level":1,"title":"Self-Hosted Sentry","text":"<p>Run Sentry on your own infrastructure using the official self-hosted installer.</p> <p>Resource requirements: 16 GB RAM minimum for Sentry alone; 24 GB+ recommended if Prometheus/Grafana/Loki run on the same host. On a 12 GB VPS, use Sentry Cloud instead.</p> <p>Sentry is not a single Docker image — the installer deploys Postgres, Redis, Kafka, ClickHouse, Snuba, Relay, web, workers, cron, and more.</p>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#install","level":2,"title":"Install","text":"<pre><code># Sibling directory to this repo, e.g. ~/DevOps/sentry-self-hosted\ngit clone https://github.com/getsentry/self-hosted.git\ncd self-hosted\n./install.sh\n</code></pre> <p>Follow the interactive installer to create an admin user and generate <code>docker-compose.yml</code>.</p> <p>Keep Sentry in its own directory — do not merge into docker-compose.monitoring.yml. Upgrades use Sentry's <code>./install.sh</code>.</p>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#expose-via-traefik","level":2,"title":"Expose via Traefik","text":"<p>After installation, configure the public URL and attach to the existing proxy network.</p>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#1-set-url-prefix","level":3,"title":"1. Set URL prefix","text":"<p>Edit <code>sentry/config.yml</code>:</p> <pre><code>system.url-prefix: 'https://sentry.musfiqdehan.com'\n</code></pre>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#2-attach-web-service-to-traefik_proxy","level":3,"title":"2. Attach web service to traefik_proxy","text":"<p>In Sentry's <code>docker-compose.yml</code>, on the <code>web</code> service:</p> <pre><code>services:\n  web:\n    networks:\n      - default\n      - traefik_proxy\n    labels:\n      - traefik.enable=true\n      - traefik.http.routers.sentry.rule=Host(`sentry.musfiqdehan.com`)\n      - traefik.http.routers.sentry.entrypoints=websecure\n      - traefik.http.routers.sentry.tls=true\n      - traefik.http.services.sentry.loadbalancer.server.port=9000\n\nnetworks:\n  traefik_proxy:\n    external: true\n</code></pre> <p>Ensure Traefik is running and <code>sentry.musfiqdehan.com</code> DNS points to the server.</p>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#3-create-admin-user","level":3,"title":"3. Create admin user","text":"<p>During <code>./install.sh</code> or afterward:</p> <pre><code>docker compose exec web sentry createuser\n</code></pre> <p>Disable public signup in Sentry admin after setup.</p>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#application-sdk-wiring","level":2,"title":"Application SDK wiring","text":"<p>Create one Sentry project per app in the self-hosted UI. DSN format:</p> <pre><code>https://&lt;key&gt;@sentry.musfiqdehan.com/&lt;project-id&gt;\n</code></pre> App Package Env var Django backend <code>sentry-sdk[django]</code> <code>SENTRY_DSN</code> Next.js frontend <code>@sentry/nextjs</code> <code>NEXT_PUBLIC_SENTRY_DSN</code> NestJS API <code>@sentry/nestjs</code> <code>SENTRY_DSN</code>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#startup-order-with-traefik-monitoring","level":2,"title":"Startup order (with Traefik + monitoring)","text":"<pre><code># 1. Traefik\ndocker compose --env-file .env -f docker-compose.traefik.yml up -d\n\n# 2. Monitoring\ndocker compose --env-file .env -f docker-compose.monitoring.yml up -d\n\n# 3. Sentry (from sentry-self-hosted directory)\ndocker compose up -d\n</code></pre>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#upgrades","level":2,"title":"Upgrades","text":"<p>Follow the official upgrade guide. Always run <code>./install.sh</code> from the self-hosted repo after pulling a new release tag.</p>","path":["Self-Hosted Sentry"],"tags":[]},{"location":"sentry-self-hosted/#troubleshooting","level":2,"title":"Troubleshooting","text":"Issue Check Web UI 502 <code>docker compose ps</code> — wait for Kafka/ClickHouse health Events not ingested Relay and worker containers running; DSN matches <code>system.url-prefix</code> OOM kills <code>dmesg</code> / <code>docker stats</code> — add RAM or move Sentry to a dedicated host CSRF errors <code>system.url-prefix</code> must match the browser URL exactly (https, no trailing slash)","path":["Self-Hosted Sentry"],"tags":[]},{"location":"tls-and-certificates/","level":1,"title":"TLS &amp; Certificates","text":"<p>Traefik supports two certificate sources:</p> <ol> <li>Cloudflare Origin Certificates (primary) — long-lived, loaded from certs/</li> <li>Let's Encrypt (fallback) — DNS-01 via Cloudflare API, configured in traefik.yml</li> </ol>","path":["TLS &amp; Certificates"],"tags":[]},{"location":"tls-and-certificates/#origin-certificates","level":2,"title":"Origin certificates","text":"","path":["TLS &amp; Certificates"],"tags":[]},{"location":"tls-and-certificates/#musfiqdehancom","level":3,"title":"musfiqdehan.com","text":"<p>Default cert files:</p> <ul> <li><code>certs/origin.pem</code> — public certificate</li> <li><code>certs/origin.key</code> — private key</li> </ul> <p>Loaded by dynamic/tls.yml as the default certificate.</p>","path":["TLS &amp; Certificates"],"tags":[]},{"location":"tls-and-certificates/#uk-platform-zones","level":3,"title":"UK platform zones","text":"<p>Create a separate origin certificate per zone in Cloudflare (SSL/TLS → Origin Server):</p> Zone Certificate hostnames <code>mrdfit.uk</code> <code>mrdfit.uk</code>, <code>*.mrdfit.uk</code> <code>mrderp.uk</code> <code>mrderp.uk</code>, <code>*.mrderp.uk</code> <code>mrdhrms.uk</code> <code>mrdhrms.uk</code>, <code>*.mrdhrms.uk</code> <code>mrdlms.uk</code> <code>mrdlms.uk</code>, <code>*.mrdlms.uk</code> <p>Save as e.g. <code>certs/mrdfit.pem</code> / <code>certs/mrdfit.key</code> and add matching blocks in dynamic/tls.yml.</p> <p>Never commit certificate or key files to git.</p>","path":["TLS &amp; Certificates"],"tags":[]},{"location":"tls-and-certificates/#create-an-origin-certificate","level":3,"title":"Create an origin certificate","text":"<ol> <li>Cloudflare Dashboard → SSL/TLS → Origin Server</li> <li>Create Certificate → select hostnames for the zone</li> <li>Save PEM and key into <code>certs/</code></li> </ol>","path":["TLS &amp; Certificates"],"tags":[]},{"location":"tls-and-certificates/#lets-encrypt-fallback","level":2,"title":"Let's Encrypt fallback","text":"<p>Wildcard certs require DNS-01. The <code>letsencrypt</code> resolver in traefik.yml uses <code>provider: cloudflare</code> and reads <code>CF_DNS_API_TOKEN</code>.</p> <p>Production app routers should declare explicit domains on labels:</p> <pre><code>- traefik.http.routers.api.tls.certresolver=letsencrypt\n- traefik.http.routers.api.tls.domains[0].main=mrdfit.uk\n- traefik.http.routers.api.tls.domains[0].sans=*.mrdfit.uk\n</code></pre> <p>Traefik cannot infer ACME domains from a regex-only <code>HostRegexp</code> rule.</p> <p>ACME state is stored in the Docker volume <code>traefik_letsencrypt</code> at <code>/letsencrypt</code> inside the Traefik container.</p>","path":["TLS &amp; Certificates"],"tags":[]},{"location":"tls-and-certificates/#certificate-selection-flow","level":2,"title":"Certificate selection flow","text":"<pre><code>HTTPS request → origin cert in tls.yml matches SNI?\n  Yes → serve origin cert\n  No  → router has certresolver=letsencrypt?\n          Yes → request/use LE cert for tls.domains\n          No  → default cert or TLS error\n</code></pre>","path":["TLS &amp; Certificates"],"tags":[]},{"location":"troubleshooting/","level":1,"title":"Troubleshooting","text":"","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#tls-certificates","level":2,"title":"TLS &amp; certificates","text":"Symptom Fix Browser certificate warning Confirm <code>certs/origin.pem</code> and <code>origin.key</code> exist and match Cloudflare; or set <code>CF_DNS_API_TOKEN</code> for LE fallback UK apex works, subdomains don't Add wildcard DNS <code>*.zone.uk</code> — wildcard does not cover apex and vice versa Let's Encrypt fails Check Traefik logs for ACME errors; verify Cloudflare token has DNS Edit on the zone Wrong cert served Add per-zone cert block in dynamic/tls.yml or explicit <code>tls.domains</code> on router","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#routing","level":2,"title":"Routing","text":"Symptom Fix Service unreachable Container on <code>traefik_proxy</code>? <code>traefik.enable=true</code>? 404 from Traefik Router rule / host mismatch; check labels Wrong scheme in Django Requests must pass Cloudflare → Traefik; trusted IPs in traefik.yml Stale Hostinger page Remove conflicting DNS records for the hostname","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#monitoring","level":2,"title":"Monitoring","text":"Symptom Fix Grafana 401 at edge Create <code>dynamic/.htpasswd</code> via <code>htpasswd</code> No logs in Loki Promtail needs <code>/var/lib/docker/containers</code> + docker.sock mounts Traefik target DOWN in Prometheus Restart Traefik after enabling <code>metrics.prometheus</code>; scrape <code>traefik:8080</code>","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#adms-devices","level":2,"title":"ADMS devices","text":"Symptom Fix Device cannot connect Use <code>http://</code> not <code>https://</code>; port 80 open through Cloudflare 404 on <code>/iclock</code> <code>backend-adms</code> router must use <code>web</code> entrypoint (port 80)","path":["Troubleshooting"],"tags":[]},{"location":"troubleshooting/#useful-commands","level":2,"title":"Useful commands","text":"<pre><code># Traefik logs\ndocker logs traefik --tail 100\n\n# Reload dynamic config (file provider watches automatically)\ndocker exec traefik ls /etc/traefik/dynamic/\n\n# List routers (if dashboard enabled temporarily)\ndocker logs traefik 2&gt;&amp;1 | grep -i error\n</code></pre>","path":["Troubleshooting"],"tags":[]}]}