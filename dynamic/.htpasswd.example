# Copy to .htpasswd and set credentials before starting the monitoring stack:
#
#   cp dynamic/.htpasswd.example dynamic/.htpasswd
#   htpasswd -cb dynamic/.htpasswd admin 'your-strong-password'
#
# Traefik basic-auth protects https://grafana.musfiqdehan.com in addition to
# Grafana's own admin login.
